- @topics.each do |topic|
  = link_to exam_subject_topic_chapters_path(@exam, @subject, topic), remote: true, class: 'open-hierarchy', data: {target: 'chapters'} do
    .hierarchy-item
      = topic.name