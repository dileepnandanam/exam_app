- @chapters.each do |chapter|
  = link_to exam_subject_topic_chapter_questions_path(@exam, @subject, @topic, chapter), remote: true, class: 'open-hierarchy', data: {target: 'questions'} do
    .hierarchy-item
      = chapter.name